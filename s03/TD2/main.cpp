#include <iostream>
#include "Fonctions.h"
#include "banque.h"
using namespace std;

int main()
{
    /*compte c3;
    c3.solde=600;
    c3.numCompte=1;
    if(c3.solde=100)
        cout <<"-----------un debit---------"<<endl;
    else
        cout <<"-----------------un credit-----------"<<endl;
    compte c1,c2;
    banque b;
    float montant;
    c1.saisir();
    c1.consultersolde();
    cout<<"donnez le montant a retirer: "<<endl;
   cin>>montant;
    c1.retirerargent(montant);
     c1.consultersolde();
     c2.saisir();
    c1.transfererargent(c2);
     c2.consultersolde();
     c1.consultersolde();
     b.setnomb("biat");
     b.ajouter(c1);
     b.afficherbanque();
     b.ajouter(c2);
     b.afficherbanque();*/
         //compte c1;
      int rib;
      float solde;
      compte c3;
          /*compte c1(12,200);
          compte c2(c1);
          c3=c2;
          //  compte c1(rib,solde);
     c1.consultersolde();
     c2.consultersolde();*/
     compte c3=new compte:
         c3.~compte();
     //c3.consultersolde();
    return 0;
}
